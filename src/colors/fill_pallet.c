#include <stdbool.h>
#include <stdio.h>

bool	fill_pallet(short pallet_to_fill[3], char *line);

bool	fill_pallet(short pallet_to_fill[3], char *line)

{
	pallet_to_fill[0] = ascii_to_rgb(
	printf("%s", line);
	return (true);
}
